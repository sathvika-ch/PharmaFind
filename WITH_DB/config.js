/* ============================================================
   PharmaFind — Supabase connection settings
   ------------------------------------------------------------
   1. Go to your Supabase project → Settings → API
   2. Copy the "Project URL" and the "anon public" key
   3. Paste them below, replacing the PASTE_... placeholders
   4. Save this file. Anyone with these two values can run the
      app on any laptop and share the same database.

   NOTE: the anon key is meant to be used in the browser — it is
   safe to share with your teammates. Do NOT paste the
   "service_role" key here; that one is secret.
   ============================================================ */

window.SUPABASE_URL = "PASTE_YOUR_PROJECT_URL_HERE";   // e.g. https://abcd1234.supabase.co
window.SUPABASE_KEY = "PASTE_YOUR_ANON_PUBLIC_KEY_HERE";
